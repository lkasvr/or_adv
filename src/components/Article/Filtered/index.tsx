'use client';
import { ArticlePreview } from '@/app/articles/domain/Articles';
import React from 'react';

const Filtered = (articles: ArticlePreview[]) => {
  return <div>Filtered</div>;
};

export default Filtered;
